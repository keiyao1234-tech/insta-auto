# Cloudflare Workers 設定（Phase 2: DM → Discord 通知）
# デプロイ:  npx wrangler deploy
# Secrets:  npx wrangler secret put IG_VERIFY_TOKEN
#           npx wrangler secret put DISCORD_WEBHOOK_URL
#           npx wrangler secret put IG_APP_SECRET   # 任意（X-Hub-Signature 検証用）

name = "insta-dm-discord"
main = "src/worker.js"
compatibility_date = "2024-11-01"
